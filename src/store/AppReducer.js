const initialState = {
}

const reducer = (state = initialState, action) => {
    let newState = state

    switch (action.type) {
        default:
            break
    }
    return newState
}

export default reducer